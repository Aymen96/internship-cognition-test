#!/usr/bin/env node

/**
 * Module dependencies.
 */

var app = require('../app');

/**
 * Listen on provided port, on all network interfaces.
 */

app.listen(3333, () => {
  console.log('Server running on port 3333');
});
